@page "/fizzbuzz"
@rendermode InteractiveServer

<PageTitle>FizzBuzz</PageTitle>

<h3>FizzBuzz Challenge</h3>

<div class="row my-3">
    <div class="col-md-4">
        <label>Check numbers up to:</label>
        <input type="number" class="form-control" @bind="Limit" />
    </div>
    <div class="col-md-4 d-flex align-items-end">
        <button class="btn btn-primary" @onclick="RunLoop">Run</button>
    </div>
</div>

@if (Output.Count > 0)
{
    <ul class="list-group mt-3">
        @foreach (var item in Output)
        {
            <li class="list-group-item">@item</li>
        }
    </ul>
}

@code {
    private int Limit { get; set; } = 100;
    private List<string> Output { get; set; } = new();

    private void RunLoop()
    {
        Output.Clear();
        for (int i = 0; i <= Limit; i++)
        {
            if (i % 3 == 0 && i % 5 == 0) Output.Add($"{i}: fizzbuzz");
            else if (i % 3 == 0) Output.Add($"{i}: fizz");
            else if (i % 5 == 0) Output.Add($"{i}: buzz");
            else Output.Add($"{i}");
        }
    }
}
